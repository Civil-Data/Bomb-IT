using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlaceItem : MonoBehaviour
{
    /*
    private GameObject playerObject = null;
    Vector2 currentPos = null;
    // Start is called before the first frame update
    void Start()
    {
        //Change so this could be a general script
        if (playerObject == null)
        {
            playerObject = GameObject.Find("Player1");
        }
    }

    

    // Update is called once per frame
    void Update()
    {
        Vector2 currentPos = transform.position;
        //Selected item in the inventory == objectToSpawn

        
        float x = playerObject.transform.position.x;
        float y = playerObject.transform.position.y;

    }

    void SpawnItem ()
    {
        //?? Instantiate (objectToSpawn, pos)
    }
    */
}
